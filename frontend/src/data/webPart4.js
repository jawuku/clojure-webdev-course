// Web Development with ClojureScript — Part 4: Reagent, a capstone app, and where to go next.

export const webPart4 = [
  {
    id: "first-component",
    title: "Your First Reagent Component",
    group: "Reagent — Building UIs",
    summary: "A component is just a function that returns Hiccup.",
    content: [
      { t: "p", text: "Reagent is the ClojureScript library that turns Hiccup into a live user interface. (It wraps React, the most popular UI library in the world — but you get to write plain Clojure.)" },
      { t: "p", text: "The core idea is wonderfully simple: a component is just a function that returns Hiccup. You already know how to write functions, and you already know Hiccup — so you already know how to write components." },
      { t: "reagent", code: "(defn greeting-component []\n  [:div\n   [:h2 \"Hi from a component!\"]\n   [:p \"A component is just a function that returns Hiccup.\"]])\n\n;; Render it by putting it in a vector:\n[greeting-component]" },
      { t: "h", text: "Using a component" },
      { t: "p", text: "Notice how we render it: [greeting-component] — the component name inside a vector, as if it were a tag. That tells Reagent 'render this component here'. You can drop a component anywhere you'd put an element." },
      { t: "reagent", code: "(defn logo []\n  [:span {:style {:color \"#7e9cd8\" :font-weight \"bold\"}} \"</>\"])\n\n(defn header []\n  [:div {:style {:font-size \"22px\"}}\n   [logo] \" ClojureScript Rocks\"])\n\n[header]" },
      { t: "note", text: "Two forms look similar but mean different things: [:h2 ...] renders the built-in h2 tag, while [greeting-component] renders YOUR component. Lower-case keyword = HTML tag; a symbol = a component you wrote." },
    ],
    exercises: [
      {
        prompt: "Write a component `footer` (a function of no arguments) that returns [:footer \"© 2026\"].",
        starter: "(defn footer []\n  )",
        tests: ["(= (footer) [:footer \"© 2026\"])"],
        solution: "(defn footer []\n  [:footer \"© 2026\"])",
        hint: "A no-arg function returning the Hiccup vector.",
      },
      {
        prompt: "Write a component `welcome` (a function of no arguments) that returns [:div {:class \"card\"} [:h2 \"Hi!\"]] — a div with a class attribute and a nested heading.",
        starter: "(defn welcome []\n  )",
        tests: ["(= (welcome) [:div {:class \"card\"} [:h2 \"Hi!\"]])"],
        solution: "(defn welcome []\n  [:div {:class \"card\"} [:h2 \"Hi!\"]])",
        hint: "Return Hiccup with an attribute map and a child element: [:div {:class \"card\"} [:h2 \"Hi!\"]].",
      },
    ],
  },

  {
    id: "props-composition",
    title: "Props & Composition",
    group: "Reagent — Building UIs",
    summary: "Pass data into components and combine small ones into big ones.",
    content: [
      { t: "p", text: "Components become powerful when they take arguments — often called props (properties). A prop is just a normal function parameter. This lets one component render differently depending on the data you give it." },
      { t: "reagent", code: "(defn badge [label color]\n  [:span {:style {:background color\n                  :color \"white\"\n                  :padding \"4px 10px\"\n                  :border-radius \"999px\"\n                  :margin-right \"6px\"\n                  :font-size \"13px\"}}\n   label])\n\n(defn badge-row []\n  [:div\n   [badge \"Clojure\" \"#5881d8\"]\n   [badge \"Web\" \"#98bb6c\"]\n   [badge \"Fun\" \"#ffa066\"]])\n\n[badge-row]" },
      { t: "p", text: "See how badge-row builds itself out of three badge components, each with different props? That's composition — assembling a UI from small, reusable pieces. Big apps are just many small components nested together." },
      { t: "h", text: "Props can be maps too" },
      { t: "p", text: "For components with several inputs, it's common to pass a single map — exactly the kind of data you're used to." },
      { t: "reagent", code: "(defn user-card [{:keys [name role]}]\n  [:div {:style {:border \"1px solid #7e9cd8\"\n                 :border-radius \"10px\"\n                 :padding \"12px\"\n                 :max-width \"220px\"}}\n   [:strong name]\n   [:div {:style {:color \"#9a9686\"}} role]])\n\n[user-card {:name \"Ada Lovelace\" :role \"First programmer\"}]" },
      { t: "note", text: "Destructuring the props map ({:keys [name role]}) — another skill straight from the Clojure course — keeps component code tidy and readable." },
    ],
    exercises: [
      {
        prompt: "Write `alert-box` taking a message string and returning [:div.alert message]. e.g. (alert-box \"Saved!\") => [:div.alert \"Saved!\"].",
        starter: "(defn alert-box [message]\n  )",
        tests: ["(= (alert-box \"Saved!\") [:div.alert \"Saved!\"])"],
        solution: "(defn alert-box [message]\n  [:div.alert message])",
        hint: "Return [:div.alert message].",
      },
      {
        prompt: "Write `user-chip` that takes a map with :name and :online, and returns [:span {:class (if online \"online\" \"offline\")} name]. e.g. (user-chip {:name \"Ada\" :online true}) => [:span {:class \"online\"} \"Ada\"].",
        starter: "(defn user-chip [{:keys [name online]}]\n  )",
        tests: ["(= (user-chip {:name \"Ada\" :online true}) [:span {:class \"online\"} \"Ada\"])", "(= (user-chip {:name \"Alan\" :online false}) [:span {:class \"offline\"} \"Alan\"])"],
        solution: "(defn user-chip [{:keys [name online]}]\n  [:span {:class (if online \"online\" \"offline\")} name])",
        hint: "Destructure the map with {:keys [name online]}, then pick the class with if.",
      },
    ],
  },

  {
    id: "state-atom",
    title: "State with Reactive Atoms",
    group: "Reagent — Building UIs",
    summary: "Make the UI change over time — the atom you know, now reactive.",
    content: [
      { t: "p", text: "So far our components have been static. To make a UI interactive, we need state — data that changes over time (a counter, a form value, a list of todos). Reagent stores state in a reactive atom, created with r/atom." },
      { t: "p", text: "It behaves exactly like the atom you already know — deref with @, change with swap! and reset! — but with a superpower: whenever it changes, any component that read it automatically re-renders. You never manually update the page." },
      { t: "reagent", code: "(def click-count (r/atom 0))\n\n(defn counter-component []\n  [:div\n   [:p {:style {:font-size \"22px\"}} \"Count: \" @click-count]\n   [:button {:on-click #(swap! click-count inc)} \" + \"]\n   [:button {:on-click #(swap! click-count dec)} \" - \"]\n   [:button {:on-click #(reset! click-count 0)} \"reset\"]])\n\n[counter-component]" },
      { t: "p", text: "Click the buttons above — the number updates instantly. Read the code: the component simply says 'show whatever @click-count currently is'. When swap! changes the atom, Reagent re-runs the component and updates only what changed." },
      { t: "h", text: "The mental model" },
      { t: "list", items: [
        "State lives in a reactive atom (r/atom).",
        "Components read state with @ and describe the UI for that state.",
        "Event handlers (like :on-click) change the state with swap!/reset!.",
        "Reagent re-renders automatically. You describe WHAT to show, not HOW to update the page.",
      ] },
      { t: "note", text: "This one idea — 'UI is a function of state' — is the heart of modern frontend development. Change the data, and the view follows. In Clojure it feels completely natural because you already think in terms of data and functions." },
    ],
    exercises: [
      {
        prompt: "Reagent's reactive atoms behave just like the atoms you know — here we test with a plain atom. Write `tick` that takes an atom and increments it, returning the new value.",
        starter: "(defn tick [a]\n  )",
        tests: ["(let [a (atom 4)] (tick a) (= @a 5))", "(= (tick (atom 0)) 1)"],
        solution: "(defn tick [a]\n  (swap! a inc))",
        hint: "swap! with inc returns the new value.",
      },
      {
        prompt: "Write `set-to` that resets an atom to a given value and returns the new value. e.g. (set-to (atom 5) 0) => 0.",
        starter: "(defn set-to [a v]\n  )",
        tests: ["(let [a (atom 1)] (set-to a 99) (= @a 99))", "(= (set-to (atom 5) 0) 0)"],
        solution: "(defn set-to [a v]\n  (reset! a v))",
        hint: "reset! replaces the value and returns it: (reset! a v).",
      },
    ],
  },

  {
    id: "events-forms",
    title: "Handling Events & Forms",
    group: "Reagent — Building UIs",
    summary: "Respond to clicks and capture what the user types.",
    content: [
      { t: "p", text: "Interactivity means responding to events — clicks, typing, submitting. In Hiccup you attach a handler as an attribute whose value is a function: :on-click, :on-change, :on-submit, and so on." },
      { t: "h", text: "Reading input as the user types" },
      { t: "p", text: "A text input's current text lives in the event's target value. We reach it with interop: (.. e -target -value). Store it in a reactive atom, and bind the input's :value to that atom — this is called a controlled input." },
      { t: "reagent", code: "(def text (r/atom \"\"))\n\n(defn live-form []\n  [:div\n   [:input {:type \"text\"\n            :placeholder \"Type your name\"\n            :value @text\n            :on-change #(reset! text (.. % -target -value))\n            :style {:padding \"7px 10px\" :font-size \"15px\"}}]\n   [:p (if (seq @text)\n         (str \"Hello, \" @text \"!\")\n         \"Start typing above…\")]])\n\n[live-form]" },
      { t: "p", text: "Type in the box — the greeting updates on every keystroke. The atom holds the text; the input shows the atom; :on-change writes back to the atom. A tidy little loop." },
      { t: "note", text: "The #(…) is an anonymous function and % is its argument — here the browser's event object. So #(reset! text (.. % -target -value)) means 'when the input changes, set text to the input's current value'." },
    ],
    exercises: [
      {
        prompt: "Write `event-value` that, given an event-like map {:target {:value \"hi\"}}, returns the value \"hi\". (We simulate the event as a map so it's testable.)",
        starter: "(defn event-value [e]\n  )",
        tests: ["(= (event-value {:target {:value \"hi\"}}) \"hi\")", "(= (event-value {:target {:value \"\"}}) \"\")"],
        solution: "(defn event-value [e]\n  (get-in e [:target :value]))",
        hint: "With a real DOM event you'd use (.. e -target -value); here it's a map, so use get-in.",
      },
      {
        prompt: "The live form shows a greeting only when there's text. Write `preview` that returns \"Hello, NAME!\" when the draft is non-empty, otherwise \"Start typing…\". e.g. (preview \"Ada\") => \"Hello, Ada!\".",
        starter: "(defn preview [draft]\n  )",
        tests: ["(= (preview \"Ada\") \"Hello, Ada!\")", "(= (preview \"\") \"Start typing…\")"],
        solution: "(defn preview [draft]\n  (if (seq draft)\n    (str \"Hello, \" draft \"!\")\n    \"Start typing…\"))",
        hint: "Use (seq draft) to test for content, then build the greeting with str.",
      },
    ],
  },

  {
    id: "lists-keys",
    title: "Dynamic Lists",
    group: "Reagent — Building UIs",
    summary: "Render a growing collection and add to it interactively.",
    content: [
      { t: "p", text: "Most real UIs show a collection that grows and shrinks. You render it exactly as in the Hiccup chapter — with for — and change it by swapping the atom that holds it." },
      { t: "reagent", code: "(def items (r/atom [\"Milk\" \"Bread\"]))\n(def draft (r/atom \"\"))\n\n(defn shopping []\n  [:div {:style {:max-width \"320px\"}}\n   [:h3 \"Shopping list\"]\n   [:input {:value @draft\n            :placeholder \"Add an item\"\n            :on-change #(reset! draft (.. % -target -value))\n            :style {:padding \"6px 9px\"}}]\n   [:button {:on-click #(when (seq @draft)\n                          (swap! items conj @draft)\n                          (reset! draft \"\"))\n             :style {:margin-left \"6px\"}}\n    \"Add\"]\n   [:ul\n    (for [[i it] (map-indexed vector @items)]\n      ^{:key i} [:li it])]])\n\n[shopping]" },
      { t: "p", text: "Type an item, click Add, and it appears in the list — because swap! ... conj changes the atom, and the component re-renders from the new data." },
      { t: "note", text: "New tool: map-indexed. It works just like map, but hands each element to the function together with its position — so (map-indexed vector [\"a\" \"b\"]) gives ([0 \"a\"] [1 \"b\"]). That's what the shopping list uses: destructure each [index item] pair, take the index for the :key, and render the item. Reach for map-indexed whenever you need both the element AND where it sits." },
      { t: "h", text: "About :key" },
      { t: "p", text: "When you render a list of items, give each one a unique ^{:key ...}. This helps Reagent tell items apart so it can update the page efficiently (and it avoids a console warning). Remember ^{...} is metadata from the Hiccup chapter — a label attached to the element, read by Reagent. A stable id from your data is ideal; an index works for simple cases." },
      { t: "note", text: "You now have all four pieces of interactive UI: components, props, state, and events. The next lesson combines them into a complete little app." },
    ],
    exercises: [
      {
        prompt: "Write `with-indices` that returns a vector of [index item] pairs, using map-indexed. e.g. (with-indices [\"a\" \"b\"]) => [[0 \"a\"] [1 \"b\"]].",
        starter: "(defn with-indices [items]\n  )",
        tests: ["(= (with-indices [\"a\" \"b\"]) [[0 \"a\"] [1 \"b\"]])", "(= (with-indices []) [])"],
        solution: "(defn with-indices [items]\n  (vec (map-indexed vector items)))",
        hint: "map-indexed with the vector function pairs each item with its index; wrap in vec for a vector result.",
      },
      {
        prompt: "The Add button appends the draft only when it has content. Write `add-if-any` that takes a draft string and an items vector, and returns the items with the draft appended only if the draft is non-empty. e.g. (add-if-any \"Milk\" [\"Bread\"]) => [\"Bread\" \"Milk\"], but (add-if-any \"\" [\"Bread\"]) => [\"Bread\"].",
        starter: "(defn add-if-any [draft items]\n  )",
        tests: ["(= (add-if-any \"Milk\" [\"Bread\"]) [\"Bread\" \"Milk\"])", "(= (add-if-any \"\" [\"Bread\"]) [\"Bread\"])"],
        solution: "(defn add-if-any [draft items]\n  (if (seq draft) (conj items draft) items))",
        hint: "if draft has content (seq), conj it onto items; otherwise return items unchanged.",
      },
    ],
  },

  {
    id: "todo-capstone",
    title: "Capstone: A To-Do App",
    group: "Reagent — Building UIs",
    summary: "Put it all together — add, complete and delete tasks.",
    content: [
      { t: "p", text: "Time to build something real. This to-do app uses everything from the course: state in a reactive atom, a controlled input, event handlers, list rendering with keys, and small composed components. It's fully live — try it!" },
      { t: "reagent", code: "(def todos (r/atom [{:id 1 :text \"Learn Hiccup\" :done true}\n                    {:id 2 :text \"Build a component\" :done false}]))\n(def draft (r/atom \"\"))\n(def next-id (r/atom 3))\n\n(defn add-todo! []\n  (when (seq @draft)\n    (swap! todos conj {:id @next-id :text @draft :done false})\n    (swap! next-id inc)\n    (reset! draft \"\")))\n\n(defn toggle! [id]\n  (swap! todos\n         (fn [ts]\n           (mapv (fn [t] (if (= (:id t) id) (update t :done not) t)) ts))))\n\n(defn delete! [id]\n  (swap! todos (fn [ts] (vec (remove #(= (:id %) id) ts)))))\n\n(defn todo-item [{:keys [id text done]}]\n  [:li {:style {:display \"flex\"\n                :align-items \"center\"\n                :gap \"8px\"\n                :padding \"4px 0\"}}\n   [:input {:type \"checkbox\" :checked done\n            :on-change #(toggle! id)}]\n   [:span {:style {:flex \"1\"\n                   :text-decoration (if done \"line-through\" \"none\")\n                   :color (if done \"#9a9686\" \"inherit\")}}\n    text]\n   [:button {:on-click #(delete! id)} \"delete\"]])\n\n(defn todo-app []\n  [:div {:style {:max-width \"380px\"}}\n   [:h3 \"My Todos\"]\n   [:div {:style {:display \"flex\" :gap \"6px\"}}\n    [:input {:value @draft\n             :placeholder \"What needs doing?\"\n             :on-change #(reset! draft (.. % -target -value))\n             :on-key-down #(when (= (.-key %) \"Enter\") (add-todo!))\n             :style {:flex \"1\" :padding \"7px 10px\"}}]\n    [:button {:on-click add-todo!} \"Add\"]]\n   [:ul {:style {:list-style \"none\" :padding 0}}\n    (for [t @todos]\n      ^{:key (:id t)} [todo-item t])]\n   [:p {:style {:color \"#9a9686\" :font-size \"13px\"}}\n    (str (count (filter :done @todos)) \" of \" (count @todos) \" done\")]])\n\n[todo-app]" },
      { t: "p", text: "Take a moment to read it. Every part is something you learned: maps for each todo, a vector of them in an atom, mapv/remove/update to change the data immutably, keyword access, destructuring, for to render, and small components composed together. No new magic — just the fundamentals, combined." },
      { t: "note", text: "Two tiny new helpers: mapv is map that always returns a vector (handy when your result should stay a vector, like the todos list here), and vec wraps any sequence in a vector — both used above so the collection keeps its shape after toggle and delete. Same immutability you already know: the old vector is never touched, you get a new one back." },
      { t: "note", text: "This is genuinely how production ClojureScript UIs are built. Larger apps add structure and tooling on top (next lesson), but the essence is exactly what you see here: data in an atom, components that render it, events that change it." },
    ],
    exercises: [
      {
        prompt: "A helper for the app: write `toggle-done` that flips the :done value of a todo map. e.g. (toggle-done {:done false}) => {:done true}.",
        starter: "(defn toggle-done [todo]\n  )",
        tests: ["(= (toggle-done {:id 1 :done false}) {:id 1 :done true})", "(= (:done (toggle-done {:done true})) false)"],
        solution: "(defn toggle-done [todo]\n  (update todo :done not))",
        hint: "(update todo :done not).",
      },
      {
        prompt: "The app shows how many todos are done. Write `count-done` that returns how many todos in a vector have :done true. e.g. (count-done [{:done true} {:done false}]) => 1.",
        starter: "(defn count-done [todos]\n  )",
        tests: ["(= (count-done [{:done true} {:done false} {:done true}]) 2)", "(= (count-done [{:done false}]) 0)"],
        solution: "(defn count-done [todos]\n  (count (filter :done todos)))",
        hint: "Keywords act as functions, so :done works as a filter predicate: (count (filter :done todos)).",
      },
    ],
  },

  {
    id: "beyond",
    title: "Beyond the Basics",
    group: "Building Real Apps",
    summary: "re-frame, talking to servers, routing, and real project tooling.",
    content: [
      { t: "p", text: "You can now build interactive UIs. Here's a map of where to go next — the pieces that turn a page into a full application. These samples are for reading; they need a real project build (which our in-browser sandbox can't provide)." },
      { t: "h", text: "1. re-frame — structure for bigger apps" },
      { t: "p", text: "As apps grow, sprinkling many separate atoms around gets hard to manage. re-frame is the most popular ClojureScript framework: it keeps ALL state in one place and organises change around events (what happened) and subscriptions (what a component needs to see)." },
      { t: "read", code: ";; Change state by dispatching an event...\n(re-frame/dispatch [:add-todo \"Buy milk\"])\n\n;; ...a handler describes the new state:\n(re-frame/reg-event-db\n  :add-todo\n  (fn [db [_ text]]\n    (update db :todos conj {:text text :done false})))\n\n;; ...and a component subscribes to what it needs:\n(defn todo-count []\n  (let [n @(re-frame/subscribe [:todo-count])]\n    [:span \"You have \" n \" todos\"]))" },
      { t: "h", text: "2. Talking to a server (HTTP)" },
      { t: "p", text: "Apps fetch and save data over HTTP — the request/response cycle from chapter 1. Libraries like cljs-ajax make it a one-liner; often data travels as EDN (Clojure's own data format) or JSON." },
      { t: "read", code: "(require '[ajax.core :refer [GET POST]])\n\n(GET \"/api/todos\"\n  {:handler       (fn [todos] (reset! app-state todos))\n   :error-handler (fn [err]   (js/console.error err))})" },
      { t: "h", text: "3. Client-side routing" },
      { t: "p", text: "Multi-page feel without full reloads: a router maps the URL to which component to show. (You saw a hand-rolled version of this in the Clojure course app's ?lesson= links!) reitit is a common choice." },
      { t: "read", code: "(def routes\n  [[\"/\"         :home]\n   [\"/about\"    :about]\n   [\"/todos/:id\" :todo]])" },
      { t: "h", text: "4. A real project with shadow-cljs" },
      { t: "p", text: "Instead of an in-browser interpreter, real projects compile ahead of time with shadow-cljs. It gives you fast hot-reload (save a file, the browser updates instantly), access to the whole npm ecosystem, and optimised builds for production." },
      { t: "read", code: ";; shadow-cljs.edn — project configuration\n{:source-paths [\"src\"]\n :dependencies [[reagent \"1.2.0\"]]\n :builds {:app {:target :browser\n                :output-dir \"public/js\"\n                :modules {:main {:init-fn my-app.core/init}}}}}" },
      { t: "note", text: "None of this changes the fundamentals you learned. It's the same immutable data, the same components, the same 'UI is a function of state'. These tools just help you scale that idea up to real, deployed applications." },
    ],
    exercises: [],
  },

  {
    id: "webdev-next",
    title: "What's Next",
    group: "Building Real Apps",
    summary: "You did it — now go build something.",
    content: [
      { t: "p", text: "Congratulations! You've gone from 'what is the web?' to building a live, interactive to-do app in ClojureScript. Look back at how far you came:" },
      { t: "list", items: [
        "You understand the web: clients, servers, HTTP, HTML, CSS and JavaScript.",
        "You write ClojureScript and interoperate with the browser.",
        "You describe UIs as data with Hiccup.",
        "You build reactive components with Reagent, managing state and events.",
        "You know the road ahead: re-frame, HTTP, routing and shadow-cljs.",
      ] },
      { t: "h", text: "The through-line" },
      { t: "p", text: "The reason all of this felt approachable is the reason Clojure is special: it's the same small language and the same immutable data everywhere. Whether you're summing numbers in the REPL or rendering a to-do list, you're just writing functions that transform data. You already learned the hard part in the first course." },
      { t: "h", text: "Build something of your own" },
      { t: "p", text: "The fastest way to make it stick is to build. Try extending the to-do app right here in the live editors — add a 'clear completed' button, a filter for active/done, or a counter that changes colour. Then, when you're ready, start a real project with shadow-cljs and Reagent and put it online." },
      { t: "note", text: "Keep the REPL and Cheat Sheet handy, revisit the live examples, and tinker relentlessly. That's how it all clicks. Happy hacking — and welcome to web development the Clojure way!" },
      { t: "cta", text: "Want a sharp, practical superpower for every language you'll ever use? Learn to search, match and transform text with regular expressions — taught the Clojure way, with a live Regex Golf leaderboard.", label: "Start: Master Regular Expressions", courseId: "regex" },
    ],
    exercises: [],
  },
];
